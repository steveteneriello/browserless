# Railway Environment Variables
# Set these in your Railway dashboard

# Chrome Configuration for Railway
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
PUPPETEER_EXECUTABLE_PATH=/usr/bin/google-chrome-stable

# Disable Redis for now (optional - only if you have Redis add-on)
# REDIS_URL=redis://localhost:6379

# App Configuration
NODE_ENV=production
PORT=8080
TOKEN=bananalemon

# Browser Settings for Railway
MAX_CONCURRENT_SESSIONS=1
BROWSER_TIMEOUT=30000
MAX_MEMORY_PER_BROWSER=134217728

# Disable features that require file system access
ENABLE_METRICS=false
